{
  "description": "Psychology deliberately ships no executable lifecycle hook. The portable session contract in skills/00-session-bootstrap/SKILL.md is a host-cooperated instruction repeated by each user-facing skill and agent; it is not a guaranteed lifecycle event. Do not add a hook that silently reads private context.",
  "hooks": {}
}
